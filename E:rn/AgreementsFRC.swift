//
//  AgreementsFRC.swift
//  E:rn
//
//  Created by Daniel on 10/15/16.
//  Copyright © 2016 Daniel. All rights reserved.
//

import UIKit
import CoreData

class AgreementsFRC: NSFetchedResultsController<NSManagedObject> {
    /*
    
    var fetchedResultsController: NSFetchedResultsController<NSManagedObject>!
    let moc = (UIApplication.shared.delegate as! AppDelegate).persistentContainer.viewContext
    
    func initializeFetchedResultsController() {
        let fetchRequest = NSFetchRequest<NSFetchRequestResult>(entityName: "Agreement")
        let departmentSort = NSSortDescriptor(key: "department.name", ascending: true)
        let lastNameSort = NSSortDescriptor(key: "lastName", ascending: true)
        fetchRequest.sortDescriptors = [departmentSort, lastNameSort]
        
        fetchedResultsController = NSFetchedResultsController<NSManagedObject>(fetchRequest: (fetchRequest as! NSFetchRequest<_>), managedObjectContext: moc, sectionNameKeyPath: "department.name", cacheName: "rootCache")
        fetchedResultsController.delegate = self
        
        do {
            try fetchedResultsController.performFetch()
        } catch {
            fatalError("Failed to initialize FetchedResultsController: \(error)")
        }
    }
    
 */
    
    
    

}
